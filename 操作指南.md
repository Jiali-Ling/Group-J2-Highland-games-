# Highland Games 项目 - 完整操作指南

## 📋 项目状态

✅ **已完成的功能**
- 用户注册和登录（含密码加密）
- 用户个人资料管理（包括医疗信息、紧急联系人）
- 团队创建和加入（邀请码系统）
- 赛事报名（支持个人和团队报名）
- 管理员审批工作流（批准/拒绝报名）
- GDPR 数据权利（数据导出、更正、删除账户）
- 完整的数据库结构（10个关联表）
- Docker 容器化配置
- 完整的文档（API文档、部署指南、合规性检查）

## 🚀 第一步：本地运行项目

### 1. 解决数据库锁定问题（如果存在）

如果之前运行过项目，数据库可能被锁定，需要先重置：

```cmd
reset-database.bat
```

这个脚本会：
- 停止所有 Node.js 进程
- 删除旧的数据库文件
- 清理构建缓存

### 2. 初始化数据库

```cmd
npm run setup
```

这个命令会：
- 创建数据库表结构
- 填充测试数据

### 3. 启动开发服务器

```cmd
npm run dev
```

或使用快速启动脚本：

```cmd
quick-start.bat
```

### 4. 访问网站

打开浏览器访问：http://localhost:3000

### 5. 测试账户

**普通用户账户：**
- 邮箱：`duncan@highlands.com`
- 密码：`password123`

**管理员账户：**
- 邮箱：`admin@example.com`
- 密码：`admin123`

---

## 📤 第二步：上传到 GitHub

### 1. 初始化 Git 仓库

```cmd
cd "e:\2025-2026西苏文件\Internet Technologies\期末CW02\highland-games-starter"
git init
```

### 2. 添加所有文件并提交

```cmd
git add .
git commit -m "初始提交：Highland Games 完整项目实现"
```

### 3. 在 GitHub 上创建仓库

1. 访问 https://github.com/new
2. 仓库名称：`highland-games`
3. 描述：`Highland Games 赛事管理系统 - React + Remix + Prisma`
4. 可见性：**公开**（用于课程提交）
5. **不要**初始化 README（我们已经有了）
6. 点击"Create repository"

### 4. 连接到 GitHub 并推送

```cmd
git remote add origin https://github.com/Jiali-Ling/Group-J2-Highland-games-.git
git branch -M main
git push -u origin main
```

---

## 🌐 第三步：部署到 Render（在线访问）

### 准备工作

1. 注册 Render 账号：https://render.com/
2. 连接你的 GitHub 账号

### 部署步骤

#### A. 创建 PostgreSQL 数据库

1. 在 Render 控制台点击 "New +" → "PostgreSQL"
2. 配置：
   - **Name**: `highland-games-db`
   - **Database**: `highland_games`
   - **User**: `highland_user`
   - **Region**: 选择离你最近的区域
   - **Plan**: Free（学生项目够用）
3. 点击 "Create Database"
4. **保存数据库连接信息**：
   - Internal Database URL（用于应用）
   - External Database URL（用于管理）

#### B. 创建 Web Service

1. 在 Render 控制台点击 "New +" → "Web Service"
2. 连接你的 GitHub 仓库 `highland-games`
3. 配置：
   - **Name**: `highland-games`
   - **Region**: 与数据库相同
   - **Branch**: `main`
   - **Runtime**: `Node`
   - **Build Command**: 
     ```bash
     npm install && npx prisma generate && npx prisma migrate deploy && npm run build
     ```
   - **Start Command**: 
     ```bash
     npm start
     ```
   - **Plan**: Free（够用）

#### C. 设置环境变量

在 "Environment" 部分添加：

```bash
NODE_ENV=production
DATABASE_URL=<从步骤A复制的Internal Database URL>
SESSION_SECRET=<生成一个长随机字符串>
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
APP_URL=https://highland-games.onrender.com
```

**生成 SESSION_SECRET：**
```cmd
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### D. 部署

1. 点击 "Create Web Service"
2. 等待部署完成（约5-10分钟）
3. 部署成功后，点击顶部的 URL 访问

#### E. 初始化数据库数据

部署成功后，需要填充测试数据：

1. 在 Render 控制台，进入你的 Web Service
2. 点击 "Shell" 标签
3. 运行：
   ```bash
   npx prisma db seed
   ```

### 访问你的网站

你的网站将在以下地址访问：
```
https://highland-games.onrender.com
```

---

## 🔍 功能测试清单

部署完成后，测试以下功能：

### 1. 用户注册和登录
- [ ] 注册新用户
- [ ] 登录现有用户
- [ ] 登出

### 2. 个人资料管理
- [ ] 创建/编辑个人资料
- [ ] 添加医疗信息
- [ ] 添加紧急联系人

### 3. 团队管理
- [ ] 创建新团队（获得邀请码）
- [ ] 使用邀请码加入团队
- [ ] 离开团队
- [ ] 查看团队成员

### 4. 赛事报名
- [ ] 浏览所有赛事
- [ ] 查看赛事详情
- [ ] 个人报名赛事
- [ ] 团队报名赛事
- [ ] 填写同意书（隐私、风险、准确性）

### 5. 管理员功能
- [ ] 查看待审批报名
- [ ] 批准报名
- [ ] 拒绝报名（填写原因）
- [ ] 查看统计数据

### 6. GDPR 数据权利
- [ ] 导出个人数据（DSAR）
- [ ] 提交数据更正请求
- [ ] 删除账户
- [ ] 查看同意记录
- [ ] 查看数据请求历史

### 7. 历史记录
- [ ] 查看历史获奖者

---

## 📊 项目结构说明

```
highland-games/
├── app/                          # 应用核心代码
│   ├── routes/                   # 页面路由
│   │   ├── _index.jsx           # 首页
│   │   ├── auth.jsx             # 登录/注册
│   │   ├── profile._index.jsx   # 个人资料
│   │   ├── teams._index.jsx     # 团队管理
│   │   ├── events._index.jsx    # 赛事列表
│   │   ├── events.$id.jsx       # 赛事详情
│   │   ├── events.$id.register.jsx  # 赛事报名
│   │   ├── admin._index.jsx     # 管理员面板
│   │   ├── privacy._index.jsx   # GDPR 隐私
│   │   └── winners._index.jsx   # 获奖者
│   ├── utils/                   # 工具函数
│   │   ├── db.server.js        # 数据库连接
│   │   ├── session.server.js   # 会话管理
│   │   └── email.server.js     # 邮件服务
│   └── styles/                  # CSS 样式
│
├── prisma/
│   ├── schema.prisma           # 数据库结构定义
│   ├── seed.js                 # 测试数据
│   └── migrations/             # 数据库迁移
│
├── public/                      # 静态资源
│
├── 文档/
│   ├── API_DOCUMENTATION.md               # API 接口文档
│   ├── COURSE_REQUIREMENTS_COMPLIANCE.md  # 课程要求对照
│   ├── RENDER_DEPLOYMENT.md              # 部署详细指南
│   ├── GITHUB_SETUP.md                   # GitHub 设置（英文）
│   └── 操作指南.md                        # 本文档（中文）
│
├── Dockerfile                   # Docker 配置
├── docker-compose.yml          # Docker Compose
├── package.json                # 依赖管理
└── .env.example                # 环境变量模板
```

---

## 🗄️ 数据库结构

项目包含 **10 个关联表**：

1. **User** - 用户账户（认证）
2. **UserProfile** - 用户详细资料（1:1）
3. **Team** - 团队信息
4. **TeamMember** - 团队成员关系（N:N）
5. **Event** - 赛事信息
6. **Registration** - 报名记录（含审批状态）
7. **Winner** - 历史获奖者
8. **Announcement** - 公告
9. **ConsentLog** - 同意记录（GDPR 审计）
10. **DataRequest** - 数据请求记录（GDPR）

**关系图：**
```
User 1:1 UserProfile
User 1:N Registration
User 1:N Team (owned teams)
User N:N Team (via TeamMember)
Team 1:N TeamMember
Team 1:N Registration
Event 1:N Registration
Event 1:N Winner
User 1:N ConsentLog
User 1:N DataRequest
```

---

## 🛠️ 常用命令

### 开发命令

```cmd
# 安装依赖
npm install

# 重置数据库
npm run setup

# 开发模式（热重载）
npm run dev

# 生产构建
npm run build

# 启动生产服务器
npm start

# 类型检查
npm run typecheck
```

### 数据库命令

```cmd
# 生成 Prisma Client
npx prisma generate

# 创建新迁移
npx prisma migrate dev --name 迁移名称

# 应用迁移（生产环境）
npx prisma migrate deploy

# 填充测试数据
npx prisma db seed

# 打开数据库管理界面
npx prisma studio
```

### Git 命令

```cmd
# 查看状态
git status

# 添加文件
git add .

# 提交
git commit -m "提交信息"

# 推送到 GitHub
git push origin main

# 拉取最新代码
git pull origin main

# 查看提交历史
git log --oneline
```

---

## 🎯 课程要求对照

### ✅ 必做要求（全部满足）

| 要求 | 实现情况 | 说明 |
|------|---------|------|
| React 前端 | ✅ | React 18.2.0 + Remix 2.9.2 |
| Node.js 后端 | ✅ | Node.js 18+ 服务端渲染 |
| 数据库 + 架构文档 | ✅ | SQLite/PostgreSQL + Prisma Schema |
| REST API 设计文档 | ✅ | 完整 API_DOCUMENTATION.md |
| 代码放 GitHub | ✅ | 公开仓库 |
| 数据库交互 | ✅ | 完整 CRUD 操作 |
| 在线访问 | ✅ | Render 部署 |

### ✅ 加分项（部分实现）

| 加分项 | 实现情况 | 说明 |
|--------|---------|------|
| Docker 容器化 | ✅ | Dockerfile + docker-compose.yml |
| GDPR 合规 | ✅ | 数据导出/更正/删除，同意日志 |
| 后端 API 文档 | ✅ | API_DOCUMENTATION.md |
| 部署到云平台 | ✅ | Render 免费部署 |
| 移动端响应式 | ✅ | CSS 响应式设计 |

---

## 🐛 常见问题解决

### 问题1：数据库锁定错误

**错误信息：**
```
Error: SQLITE_BUSY: database is locked
```

**解决方案：**
```cmd
reset-database.bat
npm run setup
```

### 问题2：端口被占用

**错误信息：**
```
Error: Port 3000 is already in use
```

**解决方案：**
```cmd
# 查找占用端口的进程
netstat -ano | findstr :3000

# 结束进程（替换 PID）
taskkill /PID <PID> /F
```

### 问题3：Prisma Client 未生成

**错误信息：**
```
Error: @prisma/client did not initialize yet
```

**解决方案：**
```cmd
npx prisma generate
npm run dev
```

### 问题4：Build 失败

**解决方案：**
```cmd
# 清理缓存
rmdir /s /q build
rmdir /s /q public\build
rmdir /s /q node_modules

# 重新安装
npm install
npm run build
```

### 问题5：Render 部署失败

**检查清单：**
- [ ] Build Command 正确
- [ ] Start Command 正确
- [ ] 环境变量已设置（特别是 DATABASE_URL）
- [ ] package.json 中有 "start" 脚本
- [ ] Node 版本兼容（18+）

---

## 📝 提交清单

### 课程提交时需要包含：

- [ ] **GitHub 仓库链接**
  ```
  https://github.com/Jiali-Ling/Group-J2-Highland-games-
  ```

- [ ] **在线演示链接**
  ```
  https://highland-games.onrender.com
  ```

- [ ] **测试账户**
  - 普通用户：`duncan@highlands.com` / `password123`
  - 管理员：`admin@example.com` / `admin123`

- [ ] **文档链接**
  - API 文档：`/API_DOCUMENTATION.md`
  - 技术栈分析：`/TECHNOLOGY_ANALYSIS.md`
  - 合规性检查：`/COURSE_REQUIREMENTS_COMPLIANCE.md`
  - 部署指南：`/RENDER_DEPLOYMENT.md`

- [ ] **功能演示视频/截图**（可选）
  - 用户注册流程
  - 团队创建和加入
  - 赛事报名
  - 管理员审批
  - GDPR 数据权利

- [ ] **技术栈说明**
  - 前端：React 18.2.0 + Remix 2.9.2
  - 后端：Node.js 18+ (Remix SSR)
  - 数据库：PostgreSQL (生产) / SQLite (开发)
  - ORM：Prisma 5.20.0
  - 容器化：Docker
  - 部署：Render

---

## 🎓 项目亮点

1. **完整的用户流程**
   - 注册 → 个人资料 → 团队管理 → 赛事报名 → 审批

2. **GDPR 全面合规**
   - 数据主体访问请求 (DSAR)
   - 数据更正权
   - 被遗忘权（删除账户）
   - 同意管理和审计日志

3. **专业的数据库设计**
   - 10 个规范化关联表
   - 完整的外键约束
   - 级联删除/更新
   - 索引优化

4. **现代化技术栈**
   - React 服务端渲染 (SSR)
   - 类型安全的数据库查询
   - 基于会话的安全认证
   - RESTful API 设计

5. **生产就绪**
   - Docker 容器化
   - 环境变量配置
   - 错误处理
   - 安全性最佳实践

---

## 📞 获取帮助

### 查看文档

- **API 文档**：`API_DOCUMENTATION.md`
- **部署指南**：`RENDER_DEPLOYMENT.md`
- **技术分析**：`TECHNOLOGY_ANALYSIS.md`
- **合规性检查**：`COURSE_REQUIREMENTS_COMPLIANCE.md`

### 在线资源

- Remix 文档：https://remix.run/docs
- Prisma 文档：https://www.prisma.io/docs
- React 文档：https://react.dev
- Render 文档：https://render.com/docs

---

## ✨ 成功！

完成以上步骤后，你将拥有：

✅ 功能完整的 Highland Games 管理系统  
✅ 部署在 GitHub 的源代码  
✅ 在线可访问的演示网站  
✅ 符合所有课程要求  
✅ 专业的项目文档  
✅ 可展示的作品集项目  

祝你的项目获得好成绩！🎉

---

## 🚨 重要提醒

1. **修改默认密码**：部署到生产环境前，务必修改 `.env` 中的默认管理员密码

2. **保护敏感信息**：确保 `.env` 文件不要提交到 GitHub（已在 `.gitignore` 中排除）

3. **定期备份**：Render 免费计划数据库可能会被清理，重要数据请定期备份

4. **监控使用情况**：免费计划有使用限制，超出后需升级套餐

5. **测试所有功能**：提交前完整测试所有功能流程

---

**项目创建时间：** 2024年11月  
**最后更新：** 2025年11月21日  
**版本：** 1.0.0
